const Router = require('express').Router();

module.exports = ctr => {
	let userAPI = Router();

	const { user } = ctr;

	authAPI.get('/user', (req, res) => {

	});

	authAPI.post('/user', (req, res) => {

	});
};
