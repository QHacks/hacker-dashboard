module.exports = db => {
	let userCtr = {};

	const models = {
		User
	} = require('./models');

	controller.createUser = userInfo => new Promise((resolve, reject) => {

	});

	controller.getUser = userId => new Promise((resolve, reject) => {

	});

	controller.deleteUser = userId => new Promise((resolve, reject) => {

	});

	controller.updateUser = (userId, userInfo) => new Promise((resolve, reject) => {

	});

	return userCtr;
};
